
import 'package:flutter/material.dart';

class secSreen extends StatelessWidget {
  const secSreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Content will be available here soon',textAlign: TextAlign.center,style: TextStyle(fontSize: 20),),),
    );
  }
}